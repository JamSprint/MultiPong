# MultiPong
Play multiplayer pong with a NodeJS server

# Start local server
npm install;<br>
npm start

# Start dev client
npm install;<br>
cd client;<br>
webpack;<br>
webpack-dev-server --content-base dist/
