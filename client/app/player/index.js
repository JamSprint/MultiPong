import directive from './player.directive';

let playerModule = angular.module('multipong.player', []);

playerModule.directive('mpPlayer', directive);

export default playerModule = playerModule.name